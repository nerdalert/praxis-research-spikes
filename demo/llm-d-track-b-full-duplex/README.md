# Track B Full-Duplex Demo (Redirect)

The canonical Track B demo has been migrated to use the generic full-duplex
`ext_proc` + `endpoint_selector` composition.

**Use the canonical demo:** [`../llm-d-track-b/`](../llm-d-track-b/)

This directory was the temporary staging area during the full-duplex
migration. The validated scripts and manifests have been integrated into the
canonical Track B demo at `demo/llm-d-track-b/scripts/`.
