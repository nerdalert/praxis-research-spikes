# Benchmark Artifacts

`scripts/run-benchmark.sh` creates one UTC-stamped directory per run here.

Each run contains generated Praxis configs, process logs, raw JSON measurements,
captured environment metadata, and a generated markdown summary. Review the raw
artifacts before publishing or committing benchmark results.

Large generated run directories should normally remain untracked.

